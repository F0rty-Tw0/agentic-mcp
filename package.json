{
  "name": "agentic-mcp",
  "version": "0.1.0",
  "description": "Config-driven MCP server that wraps any agentic CLI tool",
  "license": "MIT",
  "type": "module",
  "packageManager": "pnpm@10.30.0",
  "bin": {
    "agentic-mcp": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/index.ts",
    "start": "node dist/index.js",
    "test": "vitest run",
    "validate:providers": "tsx src/config/validate-providers.ts",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/",
    "lint:fix": "eslint src/ --fix"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "catalog:runtime",
    "cross-spawn": "catalog:runtime",
    "which": "catalog:runtime",
    "zod": "catalog:runtime"
  },
  "devDependencies": {
    "@types/cross-spawn": "catalog:dev",
    "@types/node": "catalog:dev",
    "tsx": "catalog:dev",
    "typescript": "catalog:dev",
    "vitest": "catalog:dev",
    "eslint": "catalog:dev",
    "eslint-config-prettier": "catalog:dev",
    "lint-suite": "catalog:dev"
  },
  "engines": {
    "node": ">=22"
  }
}
