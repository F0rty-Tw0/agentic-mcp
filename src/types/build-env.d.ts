/* eslint-disable no-underscore-dangle -- esbuild `define` injects globals with double-underscore by convention */
/* eslint-disable @typescript-eslint/naming-convention -- esbuild `define` injects globals with double-underscore by convention */
/** Injected by esbuild `define` at build time. Undefined in dev mode. */
declare const __APP_VERSION__: string | undefined;
